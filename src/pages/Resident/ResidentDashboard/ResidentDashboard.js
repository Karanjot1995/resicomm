import React, { useState, useEffect } from "react";
import { useNavigate } from "react-router-dom";

function ResidentDashboard({setIsLoggedIn}) {
  const navigate = useNavigate();
   return (
     <div className="pt-50">
      Resident Dashboard
     </div>
   );
}
 
export default ResidentDashboard;

